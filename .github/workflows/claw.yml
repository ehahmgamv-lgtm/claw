name: claw
on: workflow_dispatch
jobs:
  claw:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          curl -fsSL https://openclaw.ai/install-cli.sh | bash
          ~/.openclaw/bin/openclaw onboard --help
          ~/.openclaw/bin/openclaw onboard
